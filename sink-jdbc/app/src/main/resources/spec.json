{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "sink-jdbc",
  "description": "the configuration specification of sink-jdbc",
  "type": "object",
  "properties": {
    "user": {
      "description": "The database user",
      "type": "string"
    },
    "password": {
      "description": "The database password",
      "type": "string"
    },
    "host": {
      "description": "The database hostname",
      "type": "string"
    },
    "port": {
      "description": "The database port",
      "type": "integer"
    },
    "stream": {
      "description": "The HStream stream",
      "type": "string"
    },
    "database": {
      "description": "The target database name",
      "type": "string"
    },
    "table": {
      "description": "The target database name",
      "type": "string"
    }
  },
  "required": [ "user", "password", "host", "port", "stream", "database", "table" ]
}
